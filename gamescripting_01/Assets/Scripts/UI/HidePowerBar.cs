using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HidePowerBar : MonoBehaviour {
	float FillAmt = PowerCircle.fillAmt;
	void Update () {
		if((FillAmt > 0) && (FillAmt < 1)) {

		}

	}
}
